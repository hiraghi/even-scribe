{
  "_comment": "Copy this file to release/.auth-credentials.json (gitignored) and fill in your Even Hub developer-account email and password to enable unattended login for `npm run release`. Alternatively set the EVENHUB_EMAIL and EVENHUB_PASSWORD environment variables instead of using this file. The real file is never committed and is only read by release/release.mjs at runtime.",
  "email": "you@example.com",
  "password": "your-even-hub-password"
}
