# Changelog

エンドユーザー向け変更点まとめ。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、バージョン番号は
[セマンティック バージョニング](https://semver.org/lang/ja/) に従います。

## [0.6.1] - 2026-08-03

一覧で外付けキーボードのカーソルキーが効かない問題の修正を続けています（実機調整）。

### Fixed（修正）

- ファイル一覧のキー受け取り用の隠し入力欄を、編集画面と同じ通常のテキスト入力に\
　近づけました。一部環境でカーソルキーだけがアプリに届かなかったため、実機での\
　配送を確実にするための調整です。

## [0.6.0] - 2026-08-03

外付けキーボードのカーソルキーや、フォーカスが外れてキー入力が効かなくなる問題を改善しました。

### Fixed（修正）

- ファイル一覧で外付けキーボードの `↑` / `↓` によるノート選択が効かない問題を修正\
　しました。モバイルではテキスト入力にフォーカスがある時だけカーソルキーがアプリに\
　届くため、一覧表示中は画面外の入力欄にフォーカスを保つようにしました。
- 画面をタップした後などにフォーカスが外れてキー入力が効かなくなる問題を改善しました。\
　画面をタップするたびに、その画面に応じた入力先へフォーカスを戻します\
　（スマホの画面を触ってしまった／他アプリから戻ってきた場合の復帰に有効です）。

## [0.5.0] - 2026-08-03

変換候補の取得を待たずに次の入力を続けられるようにしました（バックグラウンド変換）。

### Added（追加）

- かなを入力して `Space`→`Enter` と押したとき、候補の取得がまだ終わっていなくても、\
　生のかなで確定してしまわないようにしました。その語は `[*よみ]` の形で一時的に保留され、\
　候補が届き次第、先頭の候補へ自動的に置き換わります。ネットワークが遅い場面でも入力を\
　止めずに次の語を打ち続けられます（複数の語を同時に保留できます）。

### Changed（変更）

- 変換候補の取得中に表示していた「変換中…」の表記を「変換取得中…」に変更しました。\
　（変換そのものではなく、候補を取得している最中であることを分かりやすくするため）

## [0.4.9] - 2026-08-03

変換候補の取得中に「変換中…」と表示するようにしました。

### Added（追加）

- かな漢字変換の候補を取得している間、グラス画面に「変換中…」と表示するように\
　しました。外出先などネットワークが不安定で候補が出るまで待つ場合でも、取得中で\
　あることが分かります。

## [0.4.8] - 2026-08-03

変換の応答時間を記録する診断ログを追加しました（開発者向け）。

### Added（追加）

- かな漢字変換の候補取得にかかった時間を毎回記録する診断ログを追加しました。\
　外出先などで変換が遅いときの実測データを集めるためのもので、通常の使用には\
　影響しません（将来のオフライン簡易変換の判断材料に使います）。

## [0.4.7] - 2026-08-03

ファイルリストで物理キーボードの上下キーによるノート選択が効かない問題を修正しました。

### Fixed（修正）

- ファイルリストで物理キーボードの `↑` / `↓` によるノート選択が効かない問題を修正\
　しました。モバイルの WebView はフォーカスされた要素が無いと物理キーボードの入力を\
　ページに届けないため、一覧表示中はリストが常に入力を受け取れるようにしました。\
　これにより、ノートを開いた直後の編集エリアへも入力が届きやすくなります。

## [0.4.6] - 2026-08-02

Android でかな漢字変換の候補が出なかった問題を修正しました。

### Fixed（修正）

- Android では OS の日本語入力(Gboard)が物理キーボードの入力を横取りしてアプリに\
　届かず、アプリ内のかな漢字変換が動きませんでした。`beforeinput` から文字を取得して\
　変換に渡すようにし、Android でもかな入力・候補表示ができるようにしました。\
　(iPhone の入力挙動は変更していません)

## [0.4.5] - 2026-08-02

かな入力 ON/OFF のトグルキーが機種によって効かない問題を改善しました。

### Fixed（修正）

- `Shift+Space` / `F9` / `Ctrl+J` などのかなトグルを、OS の IME やキーボード\
　レイアウトの違い（Android で `Shift+Space` が無反応だった等）に左右されにくくしました。\
　物理キー（`event.code`）でも判定するようにしています。

## [0.4.4] - 2026-08-02

キー入力を実機上で確認できる診断用デバッグロガーを追加しました。

### Added（追加）

- 設定の「Key debug log」と `?keydebug=1` で、キーイベントの記録を画面に表示できるようにしました。\
　capture-phase で既存処理より上流のイベントを観測し、直近30件を記録します。

## [0.4.3] - 2026-08-02

かな入力の ON/OFF を `Ctrl+Space` 以外のキーでも切り替えられるようにしました。

### Added（追加）

- かな入力の ON/OFF に `Shift+Space` / `F9` / `Ctrl+J` を追加しました。\
　`Ctrl+Space` が OS 側の入力切替に取られて効かない環境でも、これらのキーで切り替えられます。\
　従来の `Ctrl+Space`・半角/全角キー・変換/無変換キーもそのまま使えます。

## [0.4.2] - 2026-08-02

リリース手順の動作確認用のメンテナンスリリース。ユーザー向けの機能変更はありません。

### Changed（変更）

- 内部のリリース・配信フローを見直しました。\
　アプリの使い方や表示に変わりはありません。

## [0.4.1] - 2026-07-21

スマホ側に上へボタンを追加。シミュレータで保存永続化がデグレしていた問題を修正。
### Added（追加）

- スマホ側のフォルダ一覧に「↑ 上へ」ボタンを追加しました。\
　タップすると親フォルダへ戻れます。

### Fixed（修正）

- ネイティブ保存領域とブラウザ保存領域をミラーリングし、シミュレータを再起動してもノート、設定、IME 学習、下書きが消えないように。\
　どちらか一方の保存領域が残る環境では、次回起動時に内容を復元できます。

## [0.4.0] - 2026-07-21

スマホからタッチ操作でノートを開けるようになり、グラス上のノートの保存を永続化しました。

### Added（追加）

- スマホ側の一覧でノートやフォルダをタップして開けるようになりました。\
　一覧の「Browse vault…」もタップしてフォルダ一覧を開けます。

### Changed（変更）

- グラスで編集中のタッチ操作を、単クリックで保存・ダブルクリックで編集終了に変更しました。\
　保存していない変更をダブルクリックで終了する場合は、従来どおり保存／破棄を確認します。
- ノート、IME の変換学習、設定、未保存の下書きを Even アプリのネイティブ保存領域へ保存するようにしました。\
　Even アプリを閉じて開き直しても、WebView のブラウザ保存領域が消えてノートが失われる問題を防ぎます。

### Fixed（修正）

- Even アプリを再起動すると保存済みノートが消えることがあった問題を修正しました。\
　大きなノートも保存できるよう、ネイティブ保存領域では約 50KB ごとに分割して記録します。

## [0.3.2] - 2026-07-20

Even Hub からインストールしたアプリが真っ黒な画面のまま何も表示されない問題を修正。

### Fixed（修正）

- Even Hub 経由でインストールしたアプリを開くと、画面が暗いまま何も表示されなかった問題を修正しました。\
　配布パッケージ（`.ehpk`）に GitHub Pages 用のビルドが入っており、アプリ本体のファイルを\
　読み込めずに真っ白（真っ黒）な画面になっていました。パッケージ用のビルドを分離して修正しています。\
　アプリの機能そのものに変更はありません。

## [0.3.1] - 2026-07-19

ブラウザ画面の「New file」ボタンを復活。グラス本体・リングの上下スクロールを、編集画面でページ送りに変更。

### Added（追加）

- ブラウザ（スマホ）側の画面に「New file」ボタンが戻りました。\
　押すと `Ctrl+N` と同じ名前入力ダイアログが開き、ノートを新規作成できます。

### Changed（変更）

- 編集画面で、グラス本体のタッチ／リングの上下スクロールが「1画面ずつのページ送り」になりました。\
　これまでは本体スクロールでもカーソルが1行ずつ動いていましたが、これからは表示だけを1画面分めくります。\
　カーソルの移動は物理キーボードの矢印キー（`↑` / `↓`）で行ってください。\
　かな漢字変換の候補が出ている間は、従来どおり本体スクロールで候補を選べます。

## [0.3.0] - 2026-07-14

ファイル・フォルダの削除機能の追加、編集画面のステータス表示の最適化、日本語入力（記号まじりの変換・ローマ字）の改善。

### Added（追加）

- ファイル・フォルダを削除できるようになりました。\
　一覧で削除したい項目を選び `Del` または `Ctrl+D` で「Delete / Cancel」の確認画面が出ます。\
　　誤操作を防ぐため、初期選択は `Cancel` です。\
　フォルダを削除すると中のファイルもまとめて削除されます。

### Changed（変更）

- 編集画面のステータス表示を整理しました。\
　IMEの状態（`[A]`/`[あ]`）・未保存マーク（`*`）・保存中表示を画面下部のヒント行へ移し、変換候補のそばで見えるようにしました。\
　上部の行・列表示は `Ln 現在行/最大行, Col 現在列` に統一しました（折り返し後の表示行で数えます）。
- 入力中に記号（`！` `？` `。` など）を打っても、直前のひらがなが即確定されなくなりました。\
　記号は変換中の文字列に取り込まれ、変換すると「かな部分だけ」変換されて記号は末尾に残ります（例:「きょう！」→ `今日！`）。\
　　変換範囲は `Shift+←/→` で調整できます。前の文節を確定しても、未確定の記号は変換待ちのまま残ります。\
　何も入力していない状態で記号を打つと、従来どおり `！`/`!`/`‼`/`⁉` から選べます。

### Fixed（修正）

- Shift＋子音キー（`LN` など）の英字が変換されず、スペースで空白になって消える問題を修正しました。
- ローマ字 `du`・`di` を「づ・ぢ」に修正しました（だぢづでど）。\
　`tcha`・`tchi`（`matcha`→「まっちゃ」）などの促音付きも変換できます。
- 別画面でフォルダ・ノートを削除した後、中のファイルが「最近のノート」に残り、開くと `File not found` になる問題を修正しました。\
　一覧に戻ると最新の状態に更新されます。
- 起動したまま保存したノートが「最近のノート」に反映されない問題を修正しました。\
　編集を終えて一覧に戻ると、保存したノートが先頭に表示されます。

## [0.2.0] - 2026-07-14

フォルダ作成機能の追加、リネーム機能の追加、ファイル・フォルダの作成／リネームのグラス画面上UIの追加。

### Added（追加）

- フォルダを作成できるようになりました（`Ctrl+Shift+N`）。
- ノート・フォルダの名前を変更（リネーム）できるようになりました（`F2`）。
- 新規作成・フォルダ作成・リネームの**名前入力をグラス画面上で**行えるようになりました。スマホの画面を見なくても、これらの操作がグラスだけで完結します。名前も日本語入力（かな漢字変換）で入力できます。
- 保存せずに編集を終了しようとすると、「保存 / 破棄」を確認するようになりました。

### Changed（変更）

- `Ctrl+N` の新規ノート作成が、名前を入力するとすぐに一覧へ反映され、作成後は一覧に戻るようになりました。（以前は編集画面に入り、一覧を開き直すまで新しいノートが一覧に表示されませんでした。）
- Shift＋英字キーで入力した英字（大文字）が、日本語入力中でも最優先の変換候補になるようになりました（`UI`・`AI`・`OOM` など）。以前は「うい」などのかな候補が優先され、英字は候補の後ろのほうにしか出ませんでした。
- リネームを始めると現在の名前が選択された状態で表示され、そのまま打ち始めると名前全体を置き換えられるようになりました。

### Fixed（修正）

- 名前を変えずにリネームを確定すると、エラー（`Path already exists`）が表示されていた問題を修正しました。変更がない場合は取り消しと同じ扱いになります。
- ファイルを開いてカーソルを動かしただけでも、次回起動時に「未保存の下書き」の復元をうながされることがあった問題を修正しました。実際に編集した内容があるときだけ、下書きの復元を案内します。

## [0.1.0] - 2026-07-12

初回リリース。かな漢字変換・グラス内保存・ノート管理・変換スタイル選択を搭載。

### Added（追加）

- 初回リリース。Even Realities G2 上で動く日本語入力メモアプリ。
- かな漢字変換による日本語入力（候補選択、文節の伸縮、変換学習）。
- ノートをグラス内に保存（オフラインで動作。変換候補の取得時のみ通信）。
- 最近のノート一覧・フォルダ表示・ノートの編集・新規ノート作成。
- 変換スタイルを Classic IME / Live suggestions から選択可能。
